-# app/views/static_pages/home.html.haml
%h1= t('welcome')
- if logged_in?
  = link_to t('work'), rating_path, class: "btn btn-primary"
- else
  %p
    = link_to t('button_labels.register'), register_path, class: "btn btn-primary"
    #{t('or')}
    = link_to t('button_labels.login'), login_path, class: "btn btn-primary"
    = t('start')
